.ico_pause
  +p-el(before, 0.25*$size, 0.7*$size)
    absolute: left 0.2*$size top 0.15*$size
    background: $color
  +p-el(after, 0.25*$size, 0.7*$size)
    absolute: right 0.2*$size top 0.15*$size
    background: $color